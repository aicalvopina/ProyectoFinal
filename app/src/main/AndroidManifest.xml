<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ec.edu.espe.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TCiudadano" />
        <activity android:name=".Cuidadano" />
        <activity android:name=".TCompetencia" />
        <activity android:name=".Competencia" />
        <activity android:name=".TOrganisacion" />
        <activity android:name=".Organizacion" />
        <activity android:name=".IncripcionCNE" />
        <activity android:name=".Periodo"></activity>
    </application>

</manifest>